package com.example.zhttaskflow.nav.route

/**
 * 首页 Feature 全局路由常量（单一事实来源）。
 *
 * 业务 feature_home 仅引用本对象，不在模块内重复定义路由字符串。
 */
object TaskFlowHomeNavRoutes {

    /** 应用首页（功能入口 hub），无路径参数。 */
    const val HOME_ROUTE: String = "app/home"
}
