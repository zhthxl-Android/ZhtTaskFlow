package com.example.zhttaskflow.nav.route

/**
 * 日志查看模块（feature_log）导航路由常量。
 *
 * 业务 feature_log 仅引用本对象，不在模块内重复定义路由字符串。
 */
object TaskFlowLogNavRoutes {

    /** 日志 Tab 根路由。 */
    const val LOG_ROUTE: String = "app/log"
}
