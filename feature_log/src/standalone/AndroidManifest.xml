<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".standalone.FeatureLogStandaloneApplication"
        android:allowBackup="true"
        android:label="@string/log_str_module_name"
        android:supportsRtl="true"
        android:theme="@style/log_Theme.Standalone">
        <activity
            android:name=".standalone.LogDebugActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/log_Theme.Standalone">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="nav"
                    android:pathPrefix="/route"
                    android:scheme="taskflow" />
            </intent-filter>
        </activity>
    </application>

</manifest>
